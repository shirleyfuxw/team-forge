{
  "name": "team-forge",
  "description": "Claude Code plugin whose product is a verified problem contract: the user's interrogated problem + verification steps the model can run without them (contract_lint enforces checkable done_when). Direct execution is the default; opt-in machinery (agent-team or workflow archetype) is forged deterministically when the contract earns it, driven by one shared runtime skill and observed via a contract-first dashboard.",
  "version": "0.10.1",
  "author": {
    "name": "shirleyfuxw"
  },
  "homepage": "https://github.com/shirleyfuxw/team-forge",
  "repository": "https://github.com/shirleyfuxw/team-forge",
  "license": "MIT",
  "keywords": [
    "agent-teams",
    "meta-extension",
    "team-forge",
    "multi-agent",
    "claude-code",
    "agent-factory"
  ]
}
